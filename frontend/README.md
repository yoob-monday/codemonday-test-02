# Lantern Library

A small Next.js App Router demo for a library lending system.

## Run

```bash
npm install
cp .env.example .env.local
npm run dev
```

Open `http://localhost:3000`.

The frontend expects the backend API at `NEXT_PUBLIC_API_BASE_URL`, which defaults in the example file to `http://localhost:3001/api`.
